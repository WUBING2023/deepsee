# DeepSee 0.6.0-alpha.14 release evidence

Status: `prepared`

| Evidence | Value |
| --- | --- |
| Canonical repository | `https://github.com/WUBING2023/deepsee` |
| Release branch | `codex/publish-deepsee-product` |
| Planned tag | `v0.6.0-alpha.14` |
| Artifact | `deepsee-0.6.0-alpha.14.tgz` |
| SHA-256 | `e2ce24f1f6569df0f6596e326f743c719bf63241b8d30c51bf128ccbc48118b1` |
| GitHub Release | `https://github.com/WUBING2023/deepsee/releases/tag/v0.6.0-alpha.14` |
| Product site | `https://deepsee-wubing2023.pages.dev` |
| Local Web acceptance | `http://127.0.0.1:3080/` returned HTTP 200 after restart |

## Verification performed

- `pnpm run release:verify` passed: TypeScript, 27 test files / 118 tests, plugin build, package smoke test, and bilingual site validation.
- The packaged artifact includes the Workflow router, DeepSee subagent provider, prebuilt Codex adapter, OCR managers, model-capability catalog, and one-command installer.
- The final package was force-refreshed into both official Web and Headless profiles as `0.6.0-alpha.14`.
- A real native Harness Workflow launched exactly one `cli:codex` child and returned `DEEPSEE_ALPHA14_OK` with exit code 0. The task used no shell fallback and modified no workspace files.
- The same-version ZIP/folder fallback now replaces a stale managed stage transactionally when `--force` is supplied; the previous complete stage is restored if replacement fails.

This record remains `prepared` until the source commit, public tag, immutable GitHub Release assets, and production Pages deployment exist. It should be advanced to `live-verified` only after public download, checksum, language routes, and one-command upgrade are checked from a clean machine.
