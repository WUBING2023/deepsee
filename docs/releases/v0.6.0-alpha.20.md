# DeepSee 0.6.0-alpha.20

DeepSee refines feedback placement so OCR installation progress stays with the visual-reading control that started it.

## Highlights

- Show OCR installation and failure feedback immediately below the visual-reading progress bar.
- Keep the OCR comparison as a compact contextual hint instead of a long sentence below the model directory.
- Show general operation feedback in a lightweight status strip below the page header.
- Keep the model directory visually stable while background OCR work continues.

## Verification

- JavaScript syntax check passed.
- UI contract tests cover contextual OCR and general feedback placement.
- 30 test files / 136 tests passed.
- Release package and website manifests passed smoke validation.
- The installed Web profile rendered general feedback directly below the page header; the model directory remained clear and undisturbed.
