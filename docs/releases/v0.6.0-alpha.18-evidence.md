# DeepSee 0.6.0-alpha.18 Verification Evidence

Verified on Windows with DeepSeek Harness `0.1.0-rc.6`.

## Automated verification

- Global memory discovery imports Claude/Codex files, deduplicates identical content, excludes Harness-native memory from duplicate injection, and strips memory text from public state.
- The CLI base adapter receives the fully assembled Harness system prompt.
- Direct Codex/Claude CLI routes and native Workflow CLI children receive inherited global memory.
- TypeScript typecheck passed.
- 30 test files / 135 tests passed.
- Final release package smoke test passed for `deepsee-0.6.0-alpha.18.tgz`.
- SHA-256: `f449def1e12a0773ad72d64277d4c0a752b2a0804eb3d06b1dcc2d28784b2853`.

## Local discovery

- The current machine exposed one non-empty `~/.claude/CLAUDE.md` source.
- DeepSee reported only its display path, source, byte count, and truncation state; the browser API did not receive its contents.

## Live Harness verification

The installed Web and Headless profiles reported `0.6.0-alpha.18`. A temporary `CLAUDE_CONFIG_DIR` containing only the non-sensitive marker `DEEPSEE_GLOBAL_MEMORY_E2E_OK` was used so the user's real memory was never sent for testing.

| Scenario | Route | Result |
| --- | --- | --- |
| New Headless base session | `deepseek-official / deepseek-v4-flash` | `DEEPSEE_GLOBAL_MEMORY_E2E_OK` |
| Direct subscription route | `opends_run_model → cli:codex / gpt-5.6-sol` | `DEEPSEE_GLOBAL_MEMORY_E2E_OK` |
| Visible native Workflow child | `workflow → cli:codex / gpt-5.6-sol` | `DEEPSEE_GLOBAL_MEMORY_E2E_OK` and Workflow status `已完成` |

The regular Web instance at `http://127.0.0.1:3080/` detected one real global source, `~/.claude/CLAUDE.md`, while returning no memory text through `/api/deepsee/v1/models`.
