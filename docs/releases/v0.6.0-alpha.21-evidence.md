# DeepSee 0.6.0-alpha.21 Verification Evidence

Verified on Windows with DeepSeek Harness `0.1.0-rc.6`.

## Product contract

- A verified Claude Code or Codex Runtime begins with one selected model.
- Additional models appear as independent rows under the same source group.
- Every row has an enabled state, a validated model choice, source metadata, and an editable capability profile.
- Added rows become independent base-model and Workflow route candidates.

## Browser and runtime verification

- `http://127.0.0.1:3080/` reported DeepSee `0.6.0-alpha.21`.
- Claude Code initially rendered one `sonnet` row and a compact `+ 模型` action.
- Adding `opus` created `cli:claude-code@2` without replacing `sonnet`.
- Both rows remained `ready` and enabled after a complete Harness restart.
- The DeepSee base-model selector listed both `sonnet` and `opus`.
- Harness' native model menu rendered one `Claude Desktop + CLI · DeepSee` provider group containing both models.
- The Kimi API group did not expose an empty CLI-only add form.
- Added routes expose independent switches, validated model selectors, removable secondary rows, and editable capability cells.

## Automated and release verification

- TypeScript typecheck passed.
- JavaScript syntax checks passed.
- 30 test files / 139 tests passed in the final package verification.
- Release smoke and website validation passed.
- Artifact: `deepsee-0.6.0-alpha.21.tgz` (`143413` bytes).
- SHA-256: `3d9ee8c735fc9d0ede092ea0a51156e8709d31f141d50b8183d7b3fd480d5a5c`.
