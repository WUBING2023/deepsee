# DeepSee 0.6.0-alpha.30 verification evidence

This file records the release checks for the explicit, dependency-free installer bootstrap.

## Release candidate

- TypeScript: passed (`tsc --noEmit`)
- Test suite: pending GitHub CI confirmation; the local desktop sandbox blocked Vitest's child-process bootstrap before test collection
- Security contract: passed; native Harness remains the credential owner and no raw-key writer ships
- Candidate artifact: `deepsee-0.6.0-alpha.30.tgz`
- Candidate SHA-256: `afb9b5b70c88240d90d12a1ab1f50ca1b5c17d084ad3b844590165401da8b665`
- Candidate size: 184365 bytes
- Packaged CLI smoke: passed through a fresh, offline npm cache
- Runtime-package staging: passed; the dependency-free bootstrap materializes the pinned Harness dependencies only inside `DSH_HOME`
- Network-backed clean install and Web boot: pending GitHub CI/public smoke because the local desktop sandbox blocks registry access
- Website validation: English and Chinese pages and `version.json` passed

## Installer evidence

- A freshly packed bootstrap starts through `npm exec --package=<artifact> -- deepsee group` with an isolated npm cache and prints its component table.
- The isolated Windows bootstrap reached the pinned Harness CLI and emitted profile-level progress; the final network-backed runtime install remains a publication gate.
- The pinned scoped Harness package is invoked as `--package=@deepseek-ai/dsh@0.1.0-rc.6 -- dsh`, so Windows does not infer `deepseek-ai` as a command.
- The published bootstrap declares no npm `dependencies` or `peerDependencies`; its staged runtime package materializes pinned Harness dependencies inside the target DSH profile.
- The exact public Release command will be smoke-tested again after the immutable GitHub asset is available.

## Public verification

- Pending publication of `v0.6.0-alpha.30`.
