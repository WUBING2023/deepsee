# DeepSee 0.6.0-alpha.17 Verification Evidence

Verified on Windows with DeepSeek Harness `0.1.0-rc.6` at `http://127.0.0.1:3080/?deepsee=0.6.0-alpha.17`.

## Browser verification

- The native DeepSee settings page initially renders only the `模型目录` summary.
- The four-column model table is absent from the visible DOM while the directory is collapsed.
- Clicking `模型目录` expands the full table with both `Claude Code 使用模型` and `Codex CLI 使用模型` selectors.
- Clicking the summary again returns the page to its compact default state.
- The Harness base-model catalog exposes only `sonnet` for the current Claude subscription and only `gpt-5.6-sol` for the current Codex subscription.
- Unselected subscription models such as `opus` and `gpt-5.6-terra` are absent from the Harness catalog while remaining available in the account selectors.
- Duplicate `deepsee-cli-*` registry rows are no longer synchronized into the DeepSee matrix.

## Automated verification

- JavaScript syntax check passed.
- TypeScript typecheck passed.
- 29 test files / 129 tests passed.
- Release package smoke test passed for `deepsee-0.6.0-alpha.17.tgz`.
- SHA-256: `9c129f0ef51dade6a9cc815fb2369e373acf0cf2da2c251ce175c368aaf7930d`.
