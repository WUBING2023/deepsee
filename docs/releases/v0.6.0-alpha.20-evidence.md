# DeepSee 0.6.0-alpha.20 Verification Evidence

Verified on Windows with DeepSeek Harness `0.1.0-rc.6`.

The final Web profile must keep OCR installation feedback inside the visual-reading preference row, directly below its progress bar. General operation feedback must appear below the page header rather than after the model directory.

## Browser verification

- `http://127.0.0.1:3080/` reported DeepSee `0.6.0-alpha.20` through its embedded API.
- The DeepSee settings page opened through the native Harness settings shell.
- Clicking `验证` rendered the result as a compact status strip directly below the page title and actions.
- The status strip appeared before initialization, visual-reading preferences, and the model directory.
- PaddleOCR completed its managed installation and rendered as `PaddleOCR · 中型` with an available `卸载` action.
- The old long OCR comparison sentence no longer appeared below the model directory.

## Automated and release verification

- JavaScript syntax check passed.
- Targeted UI contract tests passed: 16 tests.
- TypeScript typecheck passed.
- 30 test files / 136 tests passed.
- Release smoke and website validation passed.
- Artifact: `deepsee-0.6.0-alpha.20.tgz` (`140824` bytes).
- SHA-256: `7f694b5508165487089012e673fb743eda085571c2b851ff8d50adf9b5edfd54`.
