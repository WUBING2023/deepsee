# DeepSee 0.6.0-alpha.19 Verification Evidence

Verified on Windows with DeepSeek Harness `0.1.0-rc.6`.

The final Web profile must show an always-visible `模型目录` followed by separate provider/runtime cards. Each card contains the four columns `打开`, `模型`, `来源`, and `能力`; Codex and Claude cards retain their authenticated model selectors.

## Browser verification

- `http://127.0.0.1:3080/` reported DeepSee `0.6.0-alpha.19`.
- `模型目录` rendered as an ordinary region, not a `details` disclosure.
- Five source groups were visible: Kimi (`API 供应商`), Claude Desktop + CLI (`桌面端 / 本地 CLI`), Codex Desktop (`桌面端 / 本地 CLI`), Kimi CLI (`本地 CLI`), and DeepSeek (`Harness 供应商`).
- Every group rendered an accessible four-column table.
- Claude exposed `sonnet`, `opus`, `haiku`, and `fable`; Codex exposed its five verified subscription models.
- DeepSeek V4 Flash and V4 Pro appeared together under the DeepSeek provider.

## Automated and release verification

- JavaScript syntax check passed.
- TypeScript typecheck passed.
- 30 test files / 135 tests passed.
- Release smoke and website validation passed.
- Artifact: `deepsee-0.6.0-alpha.19.tgz`.
- SHA-256: `f5c73f24da114fb317eafd5a16cd80de926fa5de892059b43caf7c4e30e53831`.
